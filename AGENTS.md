# Splash Designer 共通作業ルール

## スレッド開始時

新しいスレッド・作業セッションを開始したら、依頼へ着手する前に次の3ファイルを**全文読む**。

1. `HANDOVER_TO_CODEX.md`
2. `ARCHITECTURE.md`
3. `README.md`

続けて、必ず次を確認する。

- `git status --short --branch`
- `git log --oneline --decorate -5`
- 現在の実行経路と、依頼対象ファイルの参照関係

リポジトリ本体は、このファイルがある `SplashDesigner/` ディレクトリである。1階層上をGitリポジトリと誤認しない。

## 情報の優先順位

矛盾がある場合は、次の順で判断する。

1. 最新ワークスペースの実装とGit状態
2. `ARCHITECTURE.md`
3. `HANDOVER_TO_CODEX.md`
4. `README.md`
5. 過去の会話・旧ZIP・試作物

会話の記憶だけで実装状況や完了状態を断定しない。文書が実装より古い場合は、実装を確認したうえで文書も同期する。

## Issueの進め方

- `Issue-XXXXa`: 解析。原則としてコードを変更しない。
- `Issue-XXXXb`: 修正方針と変更範囲の確定。
- `Issue-XXXXc`: 承認された内容の実装・検証。
- 枝番と実作業がずれた場合は、推測で完了扱いにせず、実際に行った内容を明記する。
- 必要十分に達したIssueや章を勝手に増築しない。追加提案は依頼範囲と分ける。
- 実行していないテストや実機確認を「確認済み」「完成」と報告しない。

## 実装原則

- 最上位原則は「機能より、ととのい」「設備がUI」「説明しない」。
- 通常のボタン、コントロールパネル、説明文、チュートリアルで体験設計を補わない。
- 既存設計を変更する前に `ARCHITECTURE.md` と実行経路を確認する。
- ユーザーの既存変更を保持し、依頼と無関係な変更・削除・大規模整理を行わない。
- 画像・音源を追加する場合は、利用条件、クレジット、配布可否を確認して記録する。
- ブラウザ音声はユーザー操作による解除、失敗時フォールバック、iOS/Safariを含む実機差を考慮する。
- GitHub Pagesで配信できる静的構成と、相対パス・大文字小文字・キャッシュ更新を意識する。

## Git運用

- 作業前に現在のブランチと未コミット差分を確認する。
- 複数ファイルにまたがる新機能は、原則として `feature/<issue>-<topic>` ブランチを使う。
- ユーザーが明示的に許可した場合は `main` へ直接コミットしてよい。
- コミット前に最低限 `git diff --check` と対象JavaScriptの構文検査を行う。
- `.DS_Store`、一時ファイル、不要な生成物をコミットしない。
- push後に `git status` と `HEAD` / `origin/main` の一致を確認する。

## コミュニケーション

- ユーザーが「作成」「修正」「続き」と依頼したら、説明を繰り返さず実行する。
- 理由や背景は、求められたとき、判断が必要なとき、または重大なリスクがあるときだけ簡潔に説明する。
- 進行中は短い進捗、完了時は成果・検証結果・残課題を明確に報告する。
- バックグラウンドで作業して後から自動返信できるような表現をしない。

