# Splash Designer Architecture

Version: Draft 0.3

## Philosophy

-   機能より、ととのい。
-   UIではなく「設備」を触る。
-   ビルド不要。Netlifyへ配置するだけで動作する。
-   HTML + CSS + JavaScript (ES Modules) + Canvas API を基本とする。

------------------------------------------------------------------------

# Directory Structure

``` text
SplashDesigner/
├── index.html
├── assets/
│   ├── img/
│   ├── se/
│   └── fonts/
└── src/
    ├── main.js
    ├── core/
    │   ├── Engine.js
    │   ├── Scene.js
    │   ├── SceneManager.js
    │   ├── Renderer.js
    │   ├── Camera.js
    │   ├── InputManager.js
    │   └── Timeline.js
    ├── scenes/
    │   ├── EntranceScene.js
    │   ├── BathScene.js
    │   └── ShareScene.js
    ├── renderers/
    │   ├── NorenRenderer.js
    │   ├── PoolRenderer.js
    │   ├── PhotoRenderer.js
    │   └── UiRenderer.js
    ├── effects/
    │   ├── RippleEngine.js
    │   ├── SplashEngine.js
    │   └── BubbleEngine.js
    ├── audio/
    │   └── AudioManager.js
    └── utils/
        └── MathUtil.js
```

------------------------------------------------------------------------

# Scene Flow

``` text
EntranceScene
      │
      ▼
BathScene
      │
      ▼
ShareScene
```

## Entrance states

```text
idle / dragging
      │ drag threshold
      ▼
entering
      │ curtain fade-out + dark interval + room fade-in
      ▼
revealed
```

- 初期表示では浴室を描画せず、暖簾の背後を暗く保つ。
- 暖簾を外側へ払うドラッグが閾値を超えたときだけ入館を開始する。
- 暖簾へのタップまたは、外側へ払うドラッグで入館を開始する。
- タップ時はタップした片側だけを中央から半分ほど軽くめくり、布の開きと入館遷移を補間する。
- 右へ払う操作は右側、左へ払う操作は左側の暖簾だけを勢いに応じて動かす。
- 入館中は暖簾の消失と浴室の出現の間に暗い時間を設ける。
- Canvasは表示領域とdevicePixelRatioに追従する。

------------------------------------------------------------------------

# Responsibilities

## EntranceScene

Responsibilities

-   暖簾の状態管理
-   ドラッグ入力受付
-   カメラ前進開始
-   BathSceneへの遷移

Does NOT

-   Canvas描画
-   水面描画
-   波紋生成

------------------------------------------------------------------------

## NorenRenderer

Responsibilities

-   布の描画
-   揺れアニメーション
-   開閉アニメーション

Implementation notes

-   暖簾は左右2枚の布メッシュで構成する
-   タップとスライドで開き量と布の変形分布を分け、タップでは中央端を軽くめくる
-   操作方向に対応する片側だけへ開き量を渡し、反対側は通常位置へ保つ
-   上辺を固定し、慣性、減衰、重力、距離拘束で布の変形を作る
-   暖簾丈は画面上端から表示高の70%を基準とする
-   下端に短い切れ目を描画しない
-   紺色の微細なグラデーション、タイトル、中央の印は一体の印刷テクスチャとして布メッシュへ写像する
-   紺色の明度は布の左右の揺れ位相に合わせて穏やかに変化させる
-   `Splash` / `Designer`は左布上部へ2行で配置する
-   中央の印は`assets/img/noren_art.png`を使用し、左右の布をまたぐ印刷テクスチャとして扱う
-   全体を平行移動させる扉のような表現には戻さない

------------------------------------------------------------------------

## PoolRenderer

Responsibilities

-   水風呂
-   水面
-   タイル
-   壁面

Implementation notes

-   浴槽の手前に立った人が、縁いっぱいの水面を斜め上から見下ろす構図で描く
-   浴槽は画面中央の縦軸に対して左右対称とし、奥辺と手前辺を水平に保つ
-   段床の奥行き線、浴槽の左右辺、水中タイルの奥行きは共通の消失点へ収束させる
-   壁面は画面高のおよそ上半分とし、浴槽を必要以上に縦長にしない
-   水風呂を画面の主役とし、浴槽縁、内壁、水中の底タイル、満水の水面を区別する
-   浴槽縁は四隅の留め継ぎ線を見せず、奥・左・右・手前が連続する太い一体の枠として描く
-   左右壁までの余白は縁の厚みに取り込み、奥壁までの距離も詰めて同じ石材面へ連続させる
-   左右縁を太くする場合も水面を縮小せず、水面を段床の外側の消失線付近まで広げる
-   外枠の手前辺は最上段の蹴上げ下端の水平線へ合わせる
-   左右縁の見かけの幅は手前縁の画面上の高さと同じピクセル寸法にし、残りの横幅を水面へ割り当てる
-   縦長・横長で水面幅を段階的に切り替えず、連続した寸法計算でレスポンシブに追従させる
-   左右縁の水平区切り線は壁際まで延長し、下側の線を水面手前端、上側に追加する線を水面奥端へ揃える
-   左右壁は外枠よりさらに外側へ逃がし、奥壁の横幅と浴槽周囲の空間を確保する
-   奥壁の床際は奥縁の外端へ直接接続し、線のない中間帯を残さない
-   4本の浴槽縁、周囲の路面、段の踏面は同一座標系の石材グラデーションを共有し、一体の設備に見せる
-   手前縁の外側はすぐ蹴上げへ落とし、手前縁を不要な水平路面へ接続しない
-   段床の目地は手前縁と奥縁へ同じ消失線上で延長し、左右縁には水平の区切り線を入れる
-   手前には浴槽へ上がる2段の床を置き、右側には一般的な逆U字型のステンレス手すりを設ける
-   手すりの手前支柱は最下段へ固定し、奥支柱は浴槽内へ伸ばして手前縁の背後へ隠す
-   手すり前後支柱の接地点は、床面上で共通の消失点へ向かう同一補助線上に置く
-   手すりは影を別部材のようにずらさず、一体の連続したステンレスパイプとして描く
-   段床の目地は踏面で消失点へ向かい、蹴上げ面では垂直に落とし、次の踏面から再び消失点へ向ける
-   2つの蹴上げは同程度の中間的な灰色とし、踏面は浴槽縁と連続する石材色に統一する
-   路面と段床の奥行き目地は密にせず、画面幅に対して3〜4区画程度を基準とする
-   最下部には独立した床面を確保し、手すりの手前支柱は壁面ではなくこの床へ接地させる
-   壁と床の境界を明確にし、同じ消失方向を持つ設備として空間を構成する
-   奥壁だけでなく左右壁を設け、浴槽側縁との間隔は奥縁と奥壁の距離を基準にする
-   左右壁の床際は共通の消失点へ向かわせる
-   一点透視の消失点は画面外ではなく、上半分を占める奥壁の中央付近に置く
-   水面の内部状態は`ReservoirSurface`が担当する

## ReservoirSurface

Responsibilities

-   格子状の高さと速度の状態保持
-   近傍状態の再帰的な伝播と減衰
-   ポインター入力および微小な環境入力の注入

Does NOT

-   浴槽、壁、タイルの描画
-   暖簾の物理計算

------------------------------------------------------------------------

## SplashEngine

Responsibilities

-   飛沫
-   波紋
-   バイブラ
-   MADMAX水流

------------------------------------------------------------------------

# Milestones

## M1

Engine Foundation

## M2

Entrance Experience

## M3

Photo + Splash

## M4

Facility Devices

-   温度計
-   バイブラ
-   MADMAX

## M5

Recording & Sharing

-   動画
-   X投稿

------------------------------------------------------------------------

# Issue Roadmap

各Issueは原則として `a: 解析` → `b: 修正方針と承認` → `c: 実装・検証・資料更新` の順で進める。

Issue-0001〜0005は、ChatGPT上で差分ZIPを受け渡しながら、起動不能だった試作を
本来の独自Engine構成へ戻した復旧Issueである。Issue-0006で入館体験まで完成した
状態が、Git管理を本格化した時点の初期`main`の土台になっている。これらは初期開発の
同番号の`Patch`とは別の管理体系であり、Gitコミットとの一対一対応は求めない。

## Issue-0001 — Browser Entry Point Recovery

Status: **完了**

Purpose: 静的Webアプリとしてブラウザから起動できる入口を復元する。

-   `index.html`と`main.js`を追加し、ES Modulesの読込経路を用意した
-   ブラウザでJavaScriptが起動する状態まで復旧した
-   当初接続したPhaser版`SplashScene`は仮デモであり、本来の実行経路ではないと後に判明した

## Issue-0002 — Independent Engine Boot Path Recovery

Status: **完了**

Purpose: Phaser版の仮起動を外し、設計資料に沿った独自Engineの起動経路を復元する。

-   `ARCHITECTURE.md`と既存クラスの参照関係を再解析した
-   正式な起動経路を`Engine → SceneManager → EntranceScene`へ戻した
-   Markdownが混入していた`Scene.js`を実行可能なJavaScriptへ復元した
-   Phaser版`Game.js`と`SplashScene`は正式経路から外したが、参照精査までは削除しない方針とした

## Issue-0003 — Camera Recovery

Status: **完了**

Purpose: 欠落していた、暖簾をくぐって浴室へ進む視点変換を復元する。

-   `EntranceScene`が要求するCamera APIを既存コードから特定した
-   `moveToZoom()`、`update()`、`begin()`、`end()`を持つ`Camera.js`を復元した
-   Cameraは各Rendererへ座標処理を持たせず、Canvas Contextの変換として前進感を与える構成とした

## Issue-0004 — Scene Lifecycle Recovery

Status: **完了**

Purpose: `SceneManager`と各Sceneのライフサイクル契約を復元する。

-   `Scene`へ`constructor(engine)`を追加し、各SceneがEngineを参照できるようにした
-   `_activate()` / `_deactivate()`を復元した
-   内部ライフサイクルから`enter()` / `leave()`を呼び出す接続を修正した

## Issue-0005 — Engine and EntranceScene Integration

Status: **完了**

Purpose: Engine、Canvas描画基盤、EntranceSceneを正しい生成順で接続する。

-   `SceneManager`ではなく`Engine`を`EntranceScene`へ渡す生成順に修正した
-   EngineへCanvas、2D Context、`renderer`を復元した
-   `requestAnimationFrame`による更新・描画ループを復元した
-   暖簾と水風呂がブラウザへ描画される状態まで到達した

## Issue-0006 — Entrance Experience

Status: **完了**

-   初期画面で水風呂を見せず、暖簾だけを主役にした
-   タップ／ドラッグによる布メッシュの開閉
-   慣性と距離拘束を使った布変形
-   前進、暖簾のフェードアウト、暗い間、浴室のフェードイン
-   レスポンシブCanvasと浴室の一点透視構図
-   浴室の基本構図と、状態を保持・伝播するリザバー型水面
-   ブラウザ表示・操作およびコンソールエラーなしを確認済み

## Issue-0007 — Audio Experience

Purpose: 暖簾操作を起点に音声を安全に有効化し、入館の手触りと浴室の空気感を作る。

Scope

-   初回のタップ／ドラッグで`AudioContext.resume()`を実行
-   暖簾の布音を操作速度に応じて再生
-   入館後に水音、遠くの桶、浴室環境音をフェードイン
-   音声ファイルがない場合や再生拒否時も体験を止めない

Out of scope

-   温度計、バイブラ、MADMAX固有音
-   勢いのある暖簾操作での隠し音声は基本音声の完成後に判断

Completion

-   初回操作後に音声が再生可能
-   入館遷移と音の開始・フェードが同期
-   自動再生制限による未処理エラーがない

## Issue-0008 — Photo Import and Composition

Purpose: ユーザーの写真をブラウザ内で読み込み、水風呂作品の素材として配置する。

Scope

-   端末からの画像選択、デコード、向き補正
-   Canvas内へのフィット、移動、拡大縮小、再選択
-   `PhotoRenderer`の責務と浴槽・水面との前後関係
-   キャンセル、非対応形式、巨大画像での安全な復帰
-   写真データを既定では端末外へ送信しない

Out of scope

-   人物の自動切り抜き
-   人物が段を上って入水するアニメーション
-   動画書き出しとSNS共有

Completion

-   対応画像を選び、浴槽体験内へ配置できる
-   再選択とキャンセルで操作不能にならない
-   スマホを含む対象環境で画像の向きと比率が崩れない

## Issue-0009 — Photo Splash Interaction

Purpose: 写真と水面へのポインター操作から、速度に応じた水の手触りを発生させる。

Scope

-   指・マウスの軌跡、速度、移動量の取得
-   波紋、水滴、飛沫、屈折、キラキラの生成
-   低速のかけ水から高速のSplash／ザバーン相当までの連続変化
-   `ReservoirSurface`と`SplashEngine`への入力
-   画面上に勢いゲージを置かず、結果そのもので強さを伝える

Out of scope

-   バイブラとMADMAXの設備効果
-   動画録画と共有

Completion

-   同じ軌跡でも速度により水の反応が変わる
-   波紋と飛沫が水面位置から発生し、時間とともに自然に減衰する
-   タッチ操作でページスクロール等と競合しない

## Issue-0010 — Water Thermometer

Status: **完了（実装・Netlifyプレビュー確認済み）**

Purpose: 奥壁の丸形温度計そのものを操作し、水風呂、シングル水風呂、風呂、熱湯を循環切り替えする。

Scope

-   奥壁中央で、床際から数えて一本目の横目地へ中心を重ねる丸形温度計
-   デジタル時は黒い盤面へ黄色の数字と小数点を表示し、`99.9`までの小数点以下1桁に対応
-   タップごとに `17.6℃ 水風呂 → 9.8℃ シングル水風呂 → 39.8℃ 風呂 → 43.6℃ 熱湯 → 17.6℃ 水風呂` と循環
-   各基準温度に対し、20秒ごとに `0.1℃` ずつだけ隣の値へ動くランダムウォークで、`-0.2℃〜+0.2℃` の揺らぎを反映
-   温度計を押したまま任意方向へスワイプすると、現在温度を示すアナログ盤面へ切り替え
-   17.6℃と9.8℃では現在の水面外観を維持
-   39.8℃では通常量、43.6℃では多量の湯気を表示
-   39.8℃・43.6℃でSplashが発生した場合は、一時的に湯気量を増加
-   湯気は奥ヘリの一列ではなく水面台形の全幅・全奥行きから疎密を持って発生し、大きさ・速度・重なりを不規則にする
-   湯気は矩形の霞レイヤーを使わず、大小の湯気が水面直上で白く重なり、上昇・拡大しながら画面上部へ薄く消えることで奥景色を霞ませる
-   画面サイズ変更後も奥壁中央と横目地の位置関係、ヒット領域を維持

Out of scope

-   バイブラとMADMAXの制御
-   通常の設定ボタン、モード名、説明文などの画面UI
-   温度による人物の表情・入浴時間の変化（別Issue）
-   水温による水面色の変更

Completion

-   タップだけで4温度を順番どおり循環できる
-   デジタル表示は昔ながらの7セグメント式とし、各桁と小数点の位置を固定したまま現在温度を小数点以下1桁で示す
-   20秒ごとの温度揺らぎが基準値±0.2℃以内に収まる
-   スワイプがタップとして誤判定されず、アナログ盤面へ切り替わる
-   温水・熱湯時の湯気量に明確な差があり、Splash増加用の受け口を持つ
-   スマホを含む各画面幅で表示・操作できる

Implementation analysis

-   現在の正式実行経路は`EntranceScene → PoolRenderer`であり、既存の`src/devices/WaterThermometer.js`と`src/scenes/DeviceLayer.js`は未接続の旧試作
-   旧試作は矩形・整数・NORMAL/SINGLEの2状態で、今回仕様とは形状・状態・配置・入力が一致しない
-   正式実装では温度状態と20秒タイマーをデバイス側、壁上の描画とヒット形状を温度計Renderer、入力のタップ／スワイプ判定を専用Controllerへ分離する
-   湯気は水面より手前、柱・手すり等との前後関係を保つ専用Rendererとして`PoolRenderer`から描画する
-   Issue-0009の速度別Splashは未実装のため、Issue-0010では湯気増加メソッドを用意し、Issue-0009で実イベントへ接続する

Confirmed interaction detail

-   スワイプはデジタル／アナログ盤面のトグルとし、どちらの盤面でもタップによる温度循環を継続する
-   アナログ盤面は0〜80℃とし、0〜20℃は5℃刻み、以降は2℃刻み。10℃ごとの太線に数字を添えるが10・30は省略する
-   アナログ目盛りの10℃区間は高温ほど広くし、15〜20℃へ青、38〜42℃へ赤のアーチ帯を目盛り背面に置く
-   目盛り配置は0℃=南南西、50℃=北北西、60℃=北北東、80℃=南南東を基準とし、色帯は太い目盛り長と同じ幅の外周扇形にする
-   アナログ盤面の数字は判読できる程度に小さく薄くし、`℃`は真南へ置く
-   アナログ主目盛りと細目盛りの線幅は揃え、黒と薄い灰色の明度差・長さの差で10℃区切りを示す
-   アナログ時は最外周の灰色を1pxだけ残し、白い盤面と目盛りを外径近くまで広げる。デジタル時の外装は維持する
-   アナログ中央下には現在温度の数値を表示せず、針だけで現在値を示す

Implementation map

-   `src/devices/WaterTemperature.js`: 4温度状態、20秒タイマー、±0.2℃揺らぎ、盤面モード
-   `src/renderers/ThermometerRenderer.js`: 奥壁の丸形デジタル／アナログ盤面と円形ヒット領域
-   `src/input/ThermometerController.js`: タップと任意方向スワイプの判別
-   `src/renderers/SteamRenderer.js`: 温水・熱湯の基礎湯気とSplash時の一時増加
-   `src/renderers/PoolRenderer.js`: 温度状態、温度計、湯気、水面入力の統合
-   `src/scenes/EntranceScene.js`: 入館後だけ有効な温度計入力の生成と破棄
-   旧矩形試作`src/devices/WaterThermometer.js`は削除し、未使用`DeviceLayer`から参照を除去

## Issue-0011 — Infrared Sensor and Vibra

Status: **完了**

Purpose: 奥ヘリの赤外線センサーを設備UIとして使い、約1分間だけ浴槽中央から泡と水流を発生させる。

Scope

-   浴槽の奥ヘリに、既存石材と同系色の黒い石材ブロックを置く
-   奥ヘリの奥行きと同じ長さを高さとする立方体を基準形状にする
-   上面は奥から奥行きの2/5まで、前面は底から高さの2/5まで残し、その2点間を立方体から45度に切り落とした傾斜面でつなぐ
-   傾斜面へ正方形のスチール板を埋め込み、中央の太い横一文字の切り込み奥で赤色LEDを明滅させる
-   円形の黒い樹脂盤は設けない
-   実操作では手かざしを想定し、画面上ではセンサー部分のタップを同じ入力として扱う
-   待機中は赤色LEDが明滅し、作動開始と同時に消灯する
-   作動開始から約60秒後に自動停止し、停止後はLEDの明滅を再開する
-   作動中は浴槽中央付近から大量の大小の泡が高速で立ち上がり、弾けるように短く消えながら水面のほぼ全体へ広がる
-   底面の空気穴を想定した細かな等間隔の発生点から泡を出し、泡群どうしに大きな空白を作らない
-   泡の発生に同期してリザバー水面へ継続入力し、水流を生む
-   作動中はバイブラの泡音を、先頭1秒と末尾1秒を除いた範囲でループ再生し、自動停止と同時に止める
-   状態が設備の見た目だけで理解できるフィードバック

Out of scope

-   汎用トグルUI
-   画面全体やカメラを揺らしてバイブラを代用する表現
-   実際の非接触・近接センサー入力（ブラウザではタップで代替）
-   MADMAX落水

Completion

-   センサーのスチール板と横一文字の切り込みが、奥ヘリの傾斜ブロック上に立体的に見える
-   待機中の赤色LED明滅、作動中の消灯、自動停止後の明滅復帰が同期する
-   センサーのタップだけで約60秒のバイブラを開始できる
-   泡と水面の反応が状態に同期
-   バイブラの作動中だけ泡音がループし、タブ復帰時も作動状態に同期して再開する
-   停止後に水面が自然に減衰する
-   停止後は約6秒かけて水面への刺激を弱め、減衰中に再起動した場合は直ちに最大振動へ戻る

0011a analysis

-   正式実行経路は `EntranceScene → PoolRenderer → ReservoirSurface`。旧 `DeviceLayer` は現在の画面へ接続されていない
-   旧 `BubbleSensor` は円形ランプを直接ボタン扱いする試作で、今回の石材ブロック、スチール板、横スリットの形状と一致しない
-   旧 `VibraEffect` は旧カメラAPIを前提に画面全体を揺らすだけで、浴槽内の泡・水流を生成しないため正式実装には使用しない
-   設備の状態と60秒タイマーは専用デバイスモデル、ブロック・スチール板・LEDは専用Renderer、タップ判定は専用Controllerへ分離する
-   泡は専用Rendererで水面台形内へ描画し、同じ進行状態から `ReservoirSurface.disturb()` へ中央起点の継続入力を与える
-   描画順は奥ヘリ石材の後、水面・手前ヘリより前に設備本体を配置し、泡は水面の反射・揺らぎと前後関係を分けて描く必要がある

Staged implementation

-   `0011a`: 現行経路、旧試作、奥ヘリ形状、入力・水面接続点を解析し、本仕様を確定する（完了）
-   `0011b`: 設備の投影形状、状態モデル、60秒タイマー、泡の広がり方、描画順、変更ファイルと検証方法を確定する（完了）
-   `0011c`: 承認された方針を実装し、構文・差分検査とNetlifyプレビューで確認した（完了）

0011b implementation plan

-   設備は温度計の付属物には見せず、奥ヘリ右側へ独立して置く。水面奥辺の中央から右端までを3分割し、右端から1区画だけ中央へ戻った位置（奥辺全幅の約83%地点）を設備中心とする。奥ヘリ幅に対して約14%の横幅とし、手すりより奥にある設備として自然な前後関係を保つ
-   黒石ブロックは奥ヘリ上へ載る立方体を基準とし、背面、奥側2/5の水平上面、手前側3/5の傾斜面、底から高さ2/5までの前面、左右側面を別ポリゴンで描く
-   投影上のブロック高は奥ヘリの奥行き寸法から求める。傾斜は上面の奥から2/5地点と前面の下から2/5地点を結び、基準立方体を45度で切断した形にする
-   設備の手前辺を奥ヘリ約83%地点へ置き、左右の奥行き線から奥側座標を浴室の消失点へ逆投影する。奥側の高さも消失点までの距離に比例して縮小し、設備だけが一点透視図法から外れないようにする
-   設備は奥ヘリと同じ石材色とし、輪郭はヘリ目地と同じ薄灰色、窓側の左側面は入射光で明るい灰色にする。設備の設置面では奥ヘリ目地を本体の背後へ隠す
-   右壁は奥壁と同じ大型石材のレンガ積み目地を一点透視図法で描き、左窓の正面にある面として奥壁より明るくする
-   スチール板は傾斜面の内側へ余白を取った台形として投影し、中央の横スリットも同じ消失方向へ沿わせる。LEDはスリット外へ描かず、切り込みの暗部の奥だけを赤く明滅させる
-   待機状態 `idle` と作動状態 `active` の2状態を持つ。`start()` はidle時だけ受理し、active中の再タップでは60秒を延長・重複させない
-   activeは60,000msで自動的にidleへ戻る。待機中だけ約320ms周期でLEDを点滅し、active中は常時消灯する
-   作動開始後約3.2秒で、泡の発生範囲を水面中央の狭い範囲からほぼ全面まで広げる。停止時は新規発生だけを止め、既存泡と水面波は個別の寿命に任せる
-   泡は水面台形上の18×11の発生点を巡回し、奥ほど小さく手前ほど大きく投影する。各点から大量の大小の塗り泡を密集・重複させ、約0.34〜0.75秒の短い寿命の末尾で膨張して弾け、長い透明グラデーションを残さず消す
-   水流は約24ms間隔で水面のほぼ全面に3点ずつ強い正負入力を行い、浴槽全体の反射と波を激しく崩す。入力位置と強さを毎回変え、規則的な同心円にはしない
-   60秒停止後は水面入力を約6秒かけて二次曲線で減衰させる。減衰中の再起動では減衰値を破棄し、そのフレームから最大入力へ戻す
-   センサー設備は奥ヘリ描画後・水面描画前、泡は水面の反射描画後かつ水面台形でclipした状態、手前ヘリ・柱・手すりより背面に描く
-   入力Controllerは設備上で始まった短いpointer操作だけをタップとして受理する。浴槽水面の長押し、温度計スワイプ、暖簾操作とはヒット領域を共有しない

0011c change map

-   `src/devices/VibraSensor.js`: idle/active、60秒タイマー、重複起動防止
-   `src/input/VibraSensorController.js`: センサー専用のタップ判定とpointer解除
-   `src/renderers/VibraSensorRenderer.js`: 黒石ブロック、傾斜面、スチール板、横スリット、LED、ヒット形状
-   `src/renderers/VibraBubbleRenderer.js`: 水面UV上の泡生成、広がり、寿命、投影描画
-   `src/renderers/PoolRenderer.js`: 設備・泡・水面継続入力の更新と描画順統合
-   `src/scenes/EntranceScene.js`: 入館後だけ有効なセンサー入力の生成・破棄
-   `src/scenes/EntranceAudioBootstrap.js`: バイブラ音の登録、開始・停止、表示復帰時の再開
-   旧 `BubbleSensor`、`VibraEffect`、`DeviceLayer` は正式経路に流用しない。0011cでは `EntranceScene` 末尾の未使用Vibra試作接続だけを除去し、将来Issue用の旧ファイル自体は別整理とする

0011c verification

-   状態モデルへ時間を直接進め、60秒未満ではactive、60秒到達時にidleへ戻ること、active中の再起動で残り時間が延びないことを確認済み
-   バイブラ停止後の6秒減衰と、減衰中の再起動で最大振動へ即復帰することを数値検査済み
-   高密度化後の泡は定常時約747粒子、検査用96区画中94区画を覆い、停止後に残存しないことを確認済み
-   主要JavaScriptの `node --check` と全差分の `git diff --check` を実行済み
-   Netlifyプレビューで設備形状、泡の密度と弾け方、水面振動と減衰、区間ループ音、クレジット表示をユーザー確認済み

## Issue-0012 — MADMAX Waterfall

Status: **0012b方針確定・0012c実装待ち**

Purpose: 奥壁のMADMAXボタンから、温度状態に応じた10秒間の強烈な打たせ水／湯を起動する。

Scope

-   奥壁中央の高い位置に、実在設備を参照したステンレス箱型のMADMAXボタンを設置する
-   正面の幅・高さは温度計の直径と同程度、奥行きは正面寸法の約1/2とする
-   左窓を光源とし、右側面へ濃い影、上面右寄りへ細く淡い影、下面右寄りへ上面より濃い細い影を置く
-   前面には中央の黒い丸縁と赤い押し込みボタン、周囲の`M A D / M A X`、四隅のワッシャー付きプラスネジを描く
-   ボタン中心を、奥壁の床側から数えて3本目の横目地へ合わせる
-   pointerdown中だけ赤いボタンを沈め、pointerupで復帰させて起動を確定する
-   起動確定後のカウントダウンを経て、上方から10秒間の強烈な打たせ水を落とす
-   現在温度が水風呂・シングルなら水、風呂・熱湯なら湯として扱う
-   風呂・熱湯の高温度帯では、着水点から通常時の約5倍密度の局所湯気を発生させる
-   MADMAXシーケンス中は温度計入力を無効化し、開始時の水／湯状態を最後まで保持する
-   着水点ではバイブラを大きく超える泡、跳ね、水滴、飛沫、水面入力を発生させ、水面の内側だけで収めない
-   打たせ水開始2秒後から11秒後まで、長押しオーバーフロー相当の流出を継続する
-   1回のシーケンス中は再入力を受け付けず、重複起動させない

Out of scope

-   LED点灯
-   通常の画面UIボタン
-   人物写真・キャラクターへの衝突と頭部での飛沫
-   キャラクター位置に応じた落水位置の自動調整

Completion

-   ボタンが壁面へ貼った正方形ではなく、奥行きと採光方向を持つステンレス箱として見える
-   参照写真どおり、中央ボタンを囲む文字と四隅のネジが小さい表示でも判別できる
-   押下、復帰、カウントダウン、10秒間の落水、停止の順序が再現される
-   落水が上端から着水点まで連続し、着水点の泡・飛沫・水面振動がバイブラより明確に強い
-   落水開始2秒後から11秒後まで、浴槽四辺と段床へのオーバーフローが途切れず続く
-   温度状態に応じて水／湯として扱われ、既存の温度・湯気状態と矛盾しない
-   高温度帯では着水点の湯気が通常時の約5倍密度になり、浴室全体ではなく衝突周辺から立ち上がる
-   MADMAX中に温度計をタップ／スワイプしても温度と表示形式が変わらない
-   連打時の重複や音声破綻がない

0012a reference-image analysis

-   参照写真は1200×1200pxで、ステンレス前面は約450×450pxの正方形として中央に写る
-   中央の黒い丸縁は前面幅の約30〜32%、赤い押し面は約18〜20%。ボタン中心は前面中心とほぼ一致する
-   文字は均等な2行組みではない。上中央の`A`をボタン上、下中央の`A`をボタン下へ置き、上段左右の`M`・`D`と下段左右の`M`・`X`が中央ボタンを囲む
-   中央ボタンが上下の文字間へ食い込み、6文字全体が前面の中央域を大きく占める。太い黒ゴシックを使い、文字ごとに個別配置する
-   四隅のネジ中心は各辺から約10%内側。明るい円形ワッシャー、暗いネジ頭、中央のプラス溝を同心円で描く
-   参照写真そのものは配信素材へ含めず、Canvasで設備形状を手続き描画するための形状資料としてのみ使う

0012a current-path analysis

-   正式経路は`EntranceScene → PoolRenderer`であり、旧`DeviceLayer`、`MadMaxButton`、`MADMAXTimeline`、`MadMaxWaterController`は未接続の試作である
-   奥壁横目地は`PoolRenderer.#wall()`が`tileHeight=max(38, height×0.075)`間隔で描く。設置中心は床側の最初の横目地から2本上へ求める
-   基準サイズは`ThermometerRenderer.geometry()`の半径を共有し、MADMAX前面の一辺を温度計直径と同程度にする
-   温度判定は`PoolRenderer.temperature.mode.id`の`cold / single / warm / hot`を参照できる
-   長押しオーバーフローは`PoolRenderer.triggerOverflow()`と`OverflowEffect`が正式経路にある。ただし9秒間の再発火では最大3重制御と流量が不安定になるため、同じRendererへ接続する保持型状態を追加する
-   落水本体、着水泡、画面外へ飛ぶ飛沫には正式経路の実装がなく、専用状態モデルとRendererが必要である
-   将来の人物衝突へ備え、落水中心線、頭部衝突点、最終着水点を分離できる座標APIにする

0012 staged implementation

-   `0012a`: 参照画像、正式経路、壁面座標、温度、オーバーフロー接続点を解析し、本仕様を記録する（完了）
-   `0012b`: 箱・文字・ネジの投影、入力状態、カウントダウン時間、10秒タイムライン、落水と着水表現、継続オーバーフローの変更範囲を確定する（完了）
-   `0012c`: 承認された内容を実装し、構文・状態モデル・Netlifyプレビューで検証する

0012b confirmed timeline

-   pointerdown中だけ赤い押し面を奥へ沈め、pointerupで元へ戻す。pointerupが前面ヒット領域内なら1回のシーケンスを確定する
-   pointerupを0秒として3秒間カウントダウンする。数字、テロップ、LEDなどの説明UIは追加しない
-   3〜13秒で打たせ水／湯を落とす。開始と停止は一瞬で切らず、設備圧が立ち上がる短い流量変化を持たせながら、実流量は10秒間を維持する
-   落水開始2秒後の5秒地点から、落水開始11秒後の14秒地点までオーバーフローを保持する
-   13秒で落水本体を止めた後も、14秒までは浴槽からあふれる水と着水後の水面・飛沫の減衰を続ける
-   14秒以降はオーバーフローを解放し、残った水膜、飛沫、水面波が自然に消えた時点で再入力を許可する
-   シーケンス中のpointer入力は無視し、カウントダウンや落水時間を延長・多重化しない
-   カウントダウン開始からsettling完了までは温度計Controllerを無効化し、開始時に記録した温度モードを固定する

0012b button geometry and input

-   `MADMAXButtonRenderer.geometry(width,height)`で温度計と同じ半径計算を使い、前面の一辺を温度計直径とほぼ同寸にする
-   前面中心Xは画面中央。中心Yは`wallBottom`より上にある横目地のうち床側から3本目に一致させる
-   箱の見かけ奥行きは前面一辺の約1/2を基準とし、左窓光源に従って右側面を最も暗く、上面右寄りを淡く、下面右寄りをやや濃く描く
-   前面はステンレスの縦方向の淡いムラを持たせるが、壁面が透ける表現にはしない
-   文字は`M`左上、`A`中央上、`D`右上、`M`左下、`A`中央下、`X`右下へ個別配置し、中央の黒縁ボタンが上下段間へ食い込む
-   四隅はワッシャー、暗いネジ頭、プラス溝の3層とし、左側のネジを窓光でわずかに明るくする
-   `MADMAXButtonController`はpointer captureを使い、押下開始、移動キャンセル、pointerup確定、pointercancel復帰を管理する

0012b waterfall and impact geometry

-   落水中心線は画面中央に固定する。上端側の流路幅はMADMAX前面幅の約60%、着水直前は揺らぎと飛沫を含め約1.4倍まで広げる
-   初期着水点は水面UVの`u=.5, v=.48`とし、将来は人物の頭部衝突点を途中へ挿入しても、最終着水点を独立して保持できる構造にする
-   落水本体は白一色の柱にせず、暗い水芯、半透明の水膜、縦筋、左右へ剥がれる水片を重ねる
-   着水部は専用の高密度な白泡、上向き・横向きの水柱、大小の飛沫粒子、画面外へ出る水滴を組み合わせ、水面台形でclipしない
-   リザバー水面へは着水中心と周辺へ毎フレーム複数の強い正負入力を行い、Issue-0011のバイブラ最大値より明確に強くする
-   風呂・熱湯モードでは着水点周囲の湯気放出密度を通常時の約5倍へ増幅する。落水経路全体や浴室全体を白く覆わず、衝突点から上昇・拡散させる

0012b sustained overflow

-   MADMAX専用の保持型オーバーフロー状態を用意し、開始時に段階的に浴槽四辺・2段床・最下部へ到達させた後、14秒地点まで流量を保つ
-   既存の最大3重`OverflowEffect`を短時間に再発火して疑似保持せず、専用状態の`levels()`を既存`OverflowRenderer`へ渡して同じ流路を再利用する
-   14秒地点で保持を解放し、水膜を短時間で減衰させる。落水停止直後に床面の水が消える不自然さを避ける

0012b state and change map

-   `src/devices/MADMAXDevice.js`: idle / pressed / countdown / falling / overflow-tail / settling、3秒・10秒・9秒のタイムライン、重複起動防止、温度スナップショット
-   `src/input/MADMAXButtonController.js`: pointerdown中の押し込み、pointerup確定、キャンセルとpointer capture解除
-   `src/renderers/MADMAXButtonRenderer.js`: 箱の奥行き、採光方向、ステンレス前面、6文字、中央ボタン、ワッシャー付きプラスネジ、ヒット形状
-   `src/effects/MADMAXOverflowEffect.js`: 5〜14秒の保持型オーバーフローと解放後の減衰
-   `src/renderers/MADMAXWaterRenderer.js`: 上端からの落水、水筋、水片、着水泡、飛沫粒子、将来の頭部衝突点API
-   `src/renderers/PoolRenderer.js`: 温度状態、MADMAX状態、落水・着水・水面入力・湯気・既存OverflowRendererの統合
-   `src/scenes/EntranceScene.js`: 入館後だけ有効なMADMAX入力の生成・破棄と、MADMAX中の温度計タップ／スワイプ無効化
-   `src/scenes/EntranceAudioBootstrap.js`: 0012c途中で提供される音源を登録できる開始・停止フック。音源未提供でも状態モデルと映像は動作させる
-   旧`MadMaxButton`、`MADMAXTimeline`、`MadMaxWaterController`、`DeviceLayer`は正式経路へ流用しない

0012c verification plan

-   状態モデルへ時間を直接進め、3秒までは落水せず、3〜13秒だけ落水し、5〜14秒だけ保持型オーバーフローが有効になることを検査する
-   pressed中の移動キャンセル、シーケンス中の再入力拒否、settling完了後の再起動を検査する
-   水風呂・シングルは水、風呂・熱湯は湯として開始時の温度状態を保持することを検査する
-   countdownからsettlingまで温度計入力が無効で、完了後に再び操作可能になることを検査する
-   風呂・熱湯では着水点の湯気密度が通常時の約5倍となり、水風呂・シングルでは追加湯気が出ないことを検査する
-   主要JavaScriptを`node --check`、全差分を`git diff --check`で検査する
-   Netlifyプレビューで縦長・横長の箱形状、文字とネジ、押し込み、落水幅、着水泡、画面外飛沫、水面強度、湯気、9秒保持オーバーフローを確認する

## Issue-0013 — Recording and Video Export

Purpose: 水面体験をUIなしの短いループ動画として書き出す。

Scope

-   Canvasの録画開始・停止
-   2〜3秒の`Splash → 波紋 → 静穏`ループ
-   録画中の操作UI・デバッグ表示の除外
-   対応ブラウザでの動画ファイル生成と保存

Out of scope

-   Xへの自動アップロード
-   長時間録画と動画編集機能

Completion

-   対応ブラウザで再生可能な動画を生成できる
-   先頭と末尾が不自然に途切れない
-   非対応環境では理由を示し、アプリ本体は継続動作する

## Issue-0014 — Sharing

Purpose: 書き出した作品をユーザー自身の投稿として共有できる導線を作る。

Scope

-   投稿文 `水風呂でととのいました。 #SplashDesigner {公開URL}` の生成
-   Web Share APIまたはX Web Intentによる共有導線
-   書き出した動画をユーザーが添付できる手順とフォールバック
-   X APIを採用する場合は実装時点の公式仕様、認証、料金、権限を再確認

Out of scope

-   ユーザーの同意なしでの投稿
-   認証情報をフロントエンドへ埋め込む実装

Completion

-   対応環境で投稿文と公開URLを共有できる
-   動画添付が自動化できない環境でも保存・添付手順が途切れない
-   キャンセルや共有失敗後も作品が失われない

## Issue-0015 — Noren Hidden Welcome

Purpose: 暖簾を勢いよく払った場合だけ、隠し演出として歓迎音声を鳴らす。

Scope

-   暖簾ドラッグ速度の判定
-   通常操作では鳴らない閾値とクールダウン
-   「いらっしゃいませー！」音声の再生
-   基本の暖簾音・入館遷移との音量調整

Out of scope

-   通常の入館操作で必ず再生する案内音声
-   画面上で隠し操作を説明するUI

Completion

-   十分に速い操作だけで再現可能
-   誤判定や連続再生が通常体験を妨げない
-   音声が利用できなくても入館処理は継続する

## Issue-0016 — Project State Documentation

Purpose: 長期開発を会話の記憶に依存させず、最新状態と設計判断をワークスペースへ残す。

Scope

-   `PROJECT_STATE.md`: 現在Issue、完了Issue、起動方法、既知問題、次作業
-   `DESIGN_HISTORY.md`: 採用・却下した設計と理由
-   `ISSUE_HISTORY.md`: Issueごとの解析、変更ファイル、検証結果
-   `AI_RULES.md`: architecture-first、推測実装禁止、最小差分、a/b/c運用
-   各Issue完了時に更新する運用規則

Out of scope

-   実装コードの自動生成
-   会話ログの全文転載

Completion

-   4資料だけで現在地、次作業、主要判断を追跡できる
-   README、ARCHITECTURE、HANDOVERとの矛盾がない
-   更新責任と更新タイミングが明記されている

## Issue-0017 — Long-Press Overflow

Status: **完了**

Purpose: 水面の長押しを、水風呂の四辺から段床へ水があふれる隠れた設備体験へつなげる。

Scope

-   入館後の水面内だけを対象とする約0.8秒の長押し判定
-   画面座標から台形の水面ローカル座標への変換
-   水面の持ち上がり、浴槽四辺の水膜、2段の落水、最下部の濡れ
-   流量の減衰と通常状態への自動復帰
-   将来の人物入水からも呼べる再利用可能なオーバーフロー起動口
-   最大3回分の重複再生と、オーバーフローごとの区間指定された落水音
-   最下段も上の路面と同じ水膜処理を使い、落水到達時に短時間で満ち、終了後はスッと引く

Out of scope

-   人物写真の読込と入水アニメーション
-   排水口や床勾配の物理シミュレーション
-   オーバーフロー専用の説明UI

Completion

-   水面以外の長押しや通常ドラッグで誤発動しない
-   水が浴槽四辺から2段を順に落ち、最下部の路面を濡らす
-   流れと濡れが自然に消え、再発動可能な通常状態へ戻る
-   前回の濡れが残っていても、新しい長押しが成立すればオーバーフローを再開できる
-   最大3回分のオーバーフローが独立した水筋と時間軸を持ち、重なって流れる
-   縦長・横長の画面で流路が段床から外れない
-   横長で最下部床が画角外になる場合は、パースと画面比率による許容挙動とする
-   最下段だけに遅い水たまり・排水アニメーションを持たせず、上の路面と一貫した速度で濡れて戻る
-   浴槽上の水膜は水面手前端で止めず、手前ヘリ下端（最初の落水開始線）まで連続して覆う

## Issue-0019 — Audio Visibility Lifecycle

Status: **完了**

Purpose: Safariのタブ・アプリ切り替え時に、浴室の音が画面外で鳴り続けたり、復帰後に消えたままになったりする状態を防ぐ。

Scope

- `visibilitychange`による全音声と桶タイマーの停止
- 復帰時は浴槽環境音だけを再開し、単発音は破棄
- iOS Safariで停止したままになる`AudioContext`の再開
- 永久に完了しない`resume()`のタイムアウトと段階的な再試行

Completion

- 別タブへ移動すると音が止まり、戻ると浴槽環境音だけが再開する
- ホーム画面や他アプリへ移動した場合も同様に停止・復帰する
- 音声を使う他アプリやMusicから戻っても復帰する
- 繰り返し切り替えても音声と桶タイマーが多重化しない

## Issue-0018 — iPhone Safari Landscape Viewport

Status: **保留（作品の完成間際に再検討）**

Purpose: iPhone Safariを横置きした際、URLバーやタブバーにより浴槽上半分しか見えない表示領域を改善する。

Notes

- WebページからSafariを任意に完全なフルスクリーンへ移行できるとは限らないため、実装時点のiOS Safari仕様を再確認する。
- 横置き対応は現時点の最優先課題ではなく、他の主要Issueが完了した段階で着手する。

Implementation candidates

- `index.html`のviewport・PWA関連設定
- `src/core/Engine.js`のvisual viewport追従
- `src/scenes/EntranceScene.js`と`PoolRenderer`の横長画角調整

## Issue-0020 — Back-Wall Water Reflection

Status: **完了**

Purpose: 規則的な格子線ではなく、奥壁の鏡像が揺れることで水面の存在と動きを表現する。

Scope

- 水面上の規則的な格子線と横波線の撤去
- 水際を軸に上下反転した奥壁タイルの鏡像
- 既存の水面波形による水平帯単位の反射の揺らぎ
- 奥側では明瞭で、手前3分の1から水色へ馴染む反射勾配
- 画面サイズ変更時だけ再生成する反射テクスチャ

Completion

- 奥壁のレンガ風模様がX位置と幅を保った上下鏡像として映る
- 斜めの分割線や規則的な水面格子が見えない
- 風や操作による波で鏡像が揺れる
- 水面下側3分の1で鏡像が徐々に薄くなる
- オーバーフロー表示と縦横の画角を壊さない

Out of scope

- 水面を深く押した際の視覚効果改善

## Issue-0021 — Credits End Roll

Status: **完了**

Purpose: 音源提供者と制作協力への謝意を、空間の雰囲気を壊さない小さなエンドロールとして提示する。

Scope

- `SE・BGM`と`SPECIAL THANKS`の小見出し
- 現在の音源クレジットと`ChatGPT / Codex`の掲載
- 表示領域を超えた場合だけ動く自動スクロール
- 外側タップとEscによる閉じる操作
- アイボリーのゴシック体と下線のないリンク表現

Completion

- 短い一覧は静止し、表示領域を超えた場合だけエンドロールする
- 一覧への操作で自動スクロールが止まり、リンクを選択できる
- 最初のリンクへ不要なフォーカス枠が表示されない
- Closeボタンなしでダイアログ外から閉じられる
- 枠上のタイトルに頼らず、内容だけでクレジット一覧と分かる

Note

- 小規模な表示改善として会話内で仕様確認しながら実装したため、a/b/cを分けずに完了した例外Issue。

## Issue-0022 — Pre-Entrance Guide

Status: **完了**

Purpose: 初期描画と音声解除の待ち時間を、誤操作を招かない簡潔な入場案内として提示する。

Scope

- 初回Canvas描画が反映されるまでの`Loading...`
- 描画完了後だけ現れる、点滅する`Tap to start`
- iPhoneの音声解除条件を満たす全画面の実button
- 開始前のOthersを含む全背面操作の遮断
- 丸枠のない、画面幅に応じて拡大する文字表示
- タップ後は中間文言を出さず、音声準備完了まで内部で操作を遮断

Completion

- 描画前に`Tap to start`が表示されない
- 点滅するのは操作待ちの`Tap to start`だけ
- 画面のどこをタップしても開始できる
- タップ後に一瞬だけ別の準備文言が表示されない
- iPhone Safariで従来どおり音声を解除できる
- ブラウザが`Loading...`を最初に表示した後で暖簾中央画像をロード・デコードし、完成したCanvasの初回描画後だけ`Tap to start`へ進む

## Issue-0023 — Left Window and Drain Grate

Status: **実装・ユーザー確認済み（PR #6マージ待ち）**

Purpose: 左壁の閉塞感をなくし、採光窓、四角柱、浴槽脇の排水グレーチングを備えた開放的な左側空間へ変更する。

Scope

- 左壁を撤去し、右壁の向かいに一点透視へ沿う明るい窓面を配置
- 奥壁左端に、浴槽奥ヘリより手前へ張り出す四角柱を配置
- 柱右端から浴槽左側へ、水際と平行に続く排水グレーチングを配置
- グレーチング右側には細い浴槽ヘリを残す
- 左側オーバーフローをグレーチング上と窓下の床面へ流し、黒い隙間へ吸い込ませる
- オーバーフロー水膜を、柱の切り欠きを持つ単一の7角形として描画

Perspective constraints

- 窓下端、柱右側面、グレーチング左端は共通の消失点へ収束させる
- 水膜の奥側左頂点は柱前面右下ではなく、柱右側面の右下へ合わせる
- 柱前面下端から窓下端まで塗り残しを作らない
- グレーチング左右で異なる水膜を重ねず、同じグラデーションで一度だけ濡らす

Implementation map

- `src/renderers/PoolRenderer.js`: 窓・柱・浴槽形状・描画順・Issue-0023用共有座標
- `src/renderers/DrainGrateRenderer.js`: グレーチング本体、スラット、吸水する黒い隙間
- `src/renderers/OverflowRenderer.js`: 7角形の左側水膜とグレーチングへ向かう流路
- `src/scenes/EntranceScene.js`、`main.js`、`index.html`: 静的配信用キャッシュ更新

Completion

- 左壁が窓へ置き換わり、柱とグレーチングが一点透視に沿って見える
- グレーチング上側・下側が浴槽の奥端・手前端へ揃う
- 左側の水が柱前面と窓下端まで届き、乾いた三角領域を残さない
- オーバーフロー時に浴槽、ヘリ、グレーチング周辺が同じ水膜色で連続して濡れる
- ユーザーがNetlifyの横長表示を含めて形状と流路を確認済み

## Dependency Order

```text
Issue-0007 Audio
      ├── Issue-0008 Photo Import
      │         ▼
      │   Issue-0009 Photo Splash
      ├── Issue-0010 Thermometer
      ├── Issue-0011 Vibra
      └── Issue-0012 MADMAX

Issue-0008〜0012
      ▼
Issue-0013 Recording
      ▼
Issue-0014 Sharing

Issue-0015 Hidden Welcome: Issue-0007完了後に独立して実施
Issue-0016 Documentation: 全Issueと並行し、以後継続運用
```
